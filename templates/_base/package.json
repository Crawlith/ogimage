{
  "name": "@og-engine/template-base",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/helpers.js",
  "types": "dist/helpers.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "node -e \"console.log('no tests')\""
  }
}
